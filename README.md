# 💻 Sistema de Gerenciamento de Produtos (CRUD em Java)

Este projeto é um sistema de console básico para gerenciamento de produtos, desenvolvido como primeiro projeto de portfólio para praticar Programação Orientada a Objetos (POO) e lógica em Java.

## ✨ Conceitos Aplicados

O projeto utiliza o padrão de arquitetura simples (Model-Service/Controller) para separar as responsabilidades e aplicar os pilares da POO:

* **POO (Programação Orientada a Objetos):**
    * **Classes e Objetos:** Criação da classe `Produto` para representar a entidade de dados.
    * **Encapsulamento:** Uso de atributos `private` e métodos `public` (Getters e Setters) para controlar o acesso e a modificação dos dados do produto.
* **Lógica e Estrutura:**
    * **Estruturas de Controle:** Uso de `do-while` para o menu principal e `switch-case` para direcionar ações do usuário.
    * **Coleções:** Uso de `ArrayList` na classe `ProdutoService` para armazenar e gerenciar a coleção de objetos `Produto`.

## 🛠️ Funcionalidades (CRUD)

A aplicação implementa as seguintes operações básicas de manipulação de dados:

* **C**reate: Adicionar um novo produto.
* **R**ead: Listar todos os produtos ou buscar um produto por ID.
* **U**pdate: Atualizar o preço de um produto existente.
* **D**elete: Remover um produto pelo seu ID.

## 🚀 Como Executar o Projeto

1.  **Clonar o Repositório:**
    ```bash
    git clone [https://github.com/kendykvwj/Sistema-Cadastro-Java-POO.git](https://github.com/kendykvwj/Sistema-Cadastro-Java-POO.git)
    ```
2.  **Abrir na IDE:** Importe o projeto para sua IDE favorita (Eclipse, VS Code, IntelliJ).
3.  **Executar:** Execute o método `main` presente na classe `app.Main`.

---

**Autor:** Kendy Valente Wakiyama Junior.
